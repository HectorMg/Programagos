var Vec2 = require('vec2');

module.exports = function(radius, points){
	var circle = []
	for (var i = 0; i < points; i++){
		circle[i] = new Vec2(Math.cos(2*Math.PI*i/points)*radius, Math.sin(2*Math.PI*i/points)*radius);
	}
	return circle;
}