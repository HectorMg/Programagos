var Vec2 = require('vec2');

module.exports = function(topLeft, bottomRight){
	var square = []
	square[0] = topLeft;
  square[1] = new Vec2(topLeft.y, bottomRight.x);
  square[2] = bottomRight;
  square[3] = new Vec2(bottomRight.y, topLeft.x);
	return square;
}
