var Vec2 = require('vec2');

module.exports = function(topLeft, bottomRight){
	var square = []
	square[0] = topLeft;
  square[1] = new Vec2(bottomRight.x, topLeft.y);
  square[2] = bottomRight;
  square[3] = new Vec2(topLeft.x, bottomRight.y);
	return square;
}
