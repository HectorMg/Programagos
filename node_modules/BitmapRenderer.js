var Composition = require('Composition');
var Bitmap = require('Bitmap');

var BitmapRenderer = new Composition;
BitmapRenderer.bitmap = new Bitmap();

BitmapRenderer.draw = function(context){
	context.drawImage(this.bitmap.dataUrl());
}

module.exports = BitmapRenderer