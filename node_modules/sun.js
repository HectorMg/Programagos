var Composition = require('Composition');
var ImageRenderer = require('ImageRenderer');
var Transform = require('Transform');

module.exports = function(){
	var sun = new Composition(
		'transform', Transform(400,0),
		'renderer', ImageRenderer('resources/sun.png', -5)
	);

	sun.update = function(){
		//Oscillate y position between 0-300. In sync with sky foreground.
		this.components.transform.localPosition.y = 150-150*Math.cos(engine.time*0.0001);
	}
	return sun;
}
